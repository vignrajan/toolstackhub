'use client';

/**
 * AdBanner – hidden until AdSense is activated.
 * All existing imports/usages remain safe (renders nothing).
 */
export default function AdBanner({ variant = 'top', className = '' }) {
  return null;
}

/**
 * AffiliateCTA – hidden until a real affiliate link is added.
 */
export function AffiliateCTA({ toolName }) {
  return null;
}